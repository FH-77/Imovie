    <% movieinfo = JSON.parse((Tmdb::Movie.detail(params[:id])).to_json) %>

    <!--JSONという形式にしている,parserで構造を見るため-->
    <%#= movieinfo.to_json %>

    予算:<%= movieinfo['table']['budget'] %>
    公開日:<%= movieinfo['table']['release_date'] %>
    写真:<%= movieinfo['table']['poster_path'] %>

