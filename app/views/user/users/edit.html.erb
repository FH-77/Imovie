<body>
  <div>
    <h1>ユーザー情報編集</h1>
    <%= form_with model: @user,url: users_path do |f| %>
     <h4>プロフィール画像</h4>
     <%= f.file_field :image, accept: "image/*" %>
     <h4>ユーザー名</h4>
     <%= f.text_field :name %>
     <%= f.submit "編集を保存" %>
    <% end %>
  </div>
</body>